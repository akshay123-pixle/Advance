install globally
npm install -y
npm install -D typescript
npx tsc --init ->>create src and dist and uncomment the code in tsconfig.json rootDir and outDir
npx tsc ->> to put files into Dist folder
node dist/index.js
